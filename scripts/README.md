# Scripts (catalog — no source)

Helper scripts in the private core are limited to local verification:
served-script extraction + syntax check, theme contrast auditing, and
bundle inspection. No build step (single-file Worker, no bundler) and
no test runner; GitHub CI plus the deploy checklist above are the
verification path.

Catalog, not code. Scripts live in the private core.
